

!function ($) {

    /* ======================================================= */

    $(function () {
        // Slideshow
        $("#responsiveslides").responsiveSlides({
            auto: true,
            pager: false,
            nav: true,
            speed: 500,
            namespace: "centered-btns"
        });
    
    });  
    
}(window.jQuery);
